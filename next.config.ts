import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */

  images: {
    domains: ["tomato-kind-boar-622.mypinata.cloud"], // 添加你的 Pinata 网关域名
  },
};

export default nextConfig;
